---
description: 需求→设计→服务端开发→前端开发协作流程；编排 product、design、backend、frontend 四个 subagent 时按此顺序与交接规范执行
alwaysApply: false
globs: .cursor/agents/*.md
---

# 需求 → 设计 → 服务端开发 → 前端开发 流程控制

当用户要求「从需求到开发走一遍」「按流程做需求设计开发」或类似**全流程协作**时，按以下顺序派发 subagent，并传递上一阶段产出物作为下一阶段输入。

## 人工确认门控

**每一阶段完成后必须等待用户/人工确认，再进入下一阶段。** 不得在未获用户明确确认时自动连续调用下一阶段 subagent。阶段完成后向用户汇报产出路径与要点，待用户确认（如「通过，进入设计」「可以开发」）或提出修改意见后再继续。

## 执行顺序

1. **阶段 1 - 需求**：调用 `/product`，传入业务目标/场景/雏形需求；产出写入 `docs/product/`；**等待用户确认** → 确认通过后进入阶段 2。
2. **阶段 2 - 设计**：调用 `/design`，传入阶段 1 的完整产出；产出写入 `docs/design/`；**等待用户确认** → 确认通过后进入阶段 3。
3. **阶段 3 - 服务端开发**：调用 `/backend`，传入阶段 1 产出 + 阶段 2 产出；代码写入项目服务端目录，文档写入 `docs/backend/`；**等待用户确认** → 确认通过后进入阶段 4。
4. **阶段 4 - 前端开发**：调用 `/frontend`，传入阶段 1 产出 + 阶段 2 产出 + 阶段 3 的 API/接口文档；代码与文档就绪后向用户汇报，以用户确认为准完成流程。

## 产物路径（各阶段产出写入位置）

- **Product**：`docs/product/`（如 `prd.md`、`requirements.md`、`user-stories-{功能名}.md`）
- **Design**：`docs/design/`（如 `design-spec.md`、`spec-{功能名}.md`）
- **Backend**：TypeScript + Next.js，代码在 Next.js 项目内（如 `app/api/`、Server Actions）；API 文档、数据模型、自测说明等在 `docs/backend/`（如 `api-spec.md`、`data-models.md`）
- **Frontend**：代码在项目前端目录（如 `src/`）；自测说明、偏差说明等文档在 `docs/frontend/`（如 `implementation-notes.md`、`deviations.md`）

下游阶段读取上游产出时，优先从上述路径读取对应文件。

## 交接要求

- 每一阶段**完成后**，将产出写入上述对应目录，向用户汇报并**等待人工确认**；**仅在用户确认通过后**再调用下一阶段。
- 调用下一阶段时在 prompt 中**显式包含**上一阶段产出路径或关键内容，避免 subagent 缺少上下文。
- 若下游 subagent 指出上游不完整，先输出待确认项，再决定回溯到 product、design 或 backend 或由用户补充。

## 详细说明

完整阶段定义、产出物格式与完成标准见 [.cursor/agents/WORKFLOW.md](.cursor/agents/WORKFLOW.md)。四个 subagent 的 prompt 内已包含各自在流程中的位置与交接说明。
