# AI Agent 对话 — 偏差与假设（deviations）

| 项 | 说明 |
|----|------|
| 智谱模型 id | `zhipu-models.ts` 中 id 需与开放平台实际可用名一致；若某 id 上游不支持，请在清单中改为当前文档列出的名称。 |
| 多模态模型 | PRD 允许选择 GLM-4V 系列，但首版前端仅文本；选此类模型时仍只发送文本，与 PRD「不做图片输入」一致。 |
| 自动滚动 | 当前实现为消息变化即滚到底部；未实现「用户上翻阅读时暂停自动滚动」的精细策略（可按设计后续增强）。 |
