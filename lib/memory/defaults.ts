export const DEFAULT_SUMMARIZATION_ENABLED = true;
export const DEFAULT_SUMMARY_TRIGGER_TURNS = 20;
export const DEFAULT_SUMMARY_RETAIN_TURNS = 6;
export const DEFAULT_SUMMARY_TRIGGER_TOKENS = 8000;
export const DEFAULT_SUMMARY_RETAIN_TOKENS = 4000;

/** Allow retain budget to exceed target by up to 10% when shrinking by whole turns. */
export const RETAIN_TOKEN_TOLERANCE = 0.1;
