// Intentionally empty: session UI is client-driven in ChatAppShell.
// router.replace('/chat/[id]') still triggers a localhost RSC request to sync the URL.
export default function ChatConversationPage() {
  return null;
}
